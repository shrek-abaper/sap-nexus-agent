# Dependencies
node_modules/

# Frontend build/typecheck output
frontend/.next/
frontend/tsconfig.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.jks
*.keystore

# Logs and generated runtime output
*.log
logs/
# Local traces, callplans, facts, eval outputs, and Gateway runtime files.
runtime/*
!runtime/.gitkeep
runtime/traces/
# Workbench durable state dev data (JSONL runs/sessions/leases/idempotency)
.workbench-data/
tmp/
temp/

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.venv/
venv/
env/
# setuptools build metadata (regenerated by `pip install -e`, must not be tracked)
*.egg-info/

# Java / JCo gateway builds
*.class
target/
build/
out/
.gradle/
gradle-app.setting

# Native SAP JCo libraries and archives (proprietary, must not be redistributed)
sapjco3.jar
libsapjco3.*
*.so
*.dll
*.dylib

# Test and evaluation artifacts
/coverage/
/reports/
/eval-results/

# Local tooling and indexes
.codegraph/
.idea/
.vscode/

# OS files
.DS_Store
Thumbs.db

# AI Tools
.agents/skills/
.claude/skills/
.codex/hooks.json
.codex/skills/
.opencode/skills/
.comet/skills/

# SDD (subagent-driven-development) local runtime artifacts
# Keep sdd/progress.md (tracked, state-recovery ledger); ignore rebuildable task/review pieces.
.superpowers/sdd/task-*
.superpowers/sdd/review-*
.superpowers/sdd/final-review-fix-report.md

# Tangential wiki archive (not core project docs)
docs/wiki/archive/

# OhMyOpenCode local session/run-continuation tracking (agent tooling artifact,
# not a project file - accidentally committed by a fix subagent in
# declarative-intent-extraction Task 17's fix round, removed in this commit)
.omo/
