{
  "suite": "derived-parameter-binding",
  "note": "Conversation-sequence cases for parameter derivation (T5, plan tasks 4.1, 7.1-7.5). Three cases are live. Two remain pending, each with a named cause and a named prerequisite - never as a known issue. The derived-parameter assertion keys (parameterProvenance / dataEdges / orderedBefore) were added by task 7.1; governedCapabilities was added by task 7.5 and restricts the governed capability set for one case by rebuilding the snapshot from a reduced document set. provenance=capability_derived is harness vocabulary mapped from source.kind=factField per correction C12.",
  "cases": [
    {
      "id": "derived-not-asked",
      "pending": true,
      "todo": "UNREACHABLE THROUGH THE RULE PARSER, cause named. For BOTH unit and purchasing_group to be derived, the utterance must omit both - but quantity's extraction is coupled to an adjacent RECOGNISED unit token from the closed set {EA,PC,KG,G,L,M} (registry inputs[quantity].extraction, resolver: quantity), so dropping the unit also drops quantity, which is required and NOT derivable. Verified against the parser: '建 100 箱采购申请' and '建 100 TON 采购申请' and '数量 100' all yield CLARIFY missing=['quantity']. Prerequisite to make this case live: let the quantity matcher accept a bare number after 数量, which changes the extraction layer batch T deliberately left alone. THE PROPERTY ITSELF IS ASSERTED, at the planner layer rather than through the parser: agent/tests/test_planner_plan_compiler_v2.py::test_a_fully_specified_derived_plan_is_valid_and_still_demands_approval supplies material+plant+quantity+delivery_date and asserts BOTH unit and purchasing_group bind from factField while write_side_effect and approval_required remain. So this pending case is a missing conversational entry point, not a missing capability.",
      "userQuery": "给 DEMOA2 在 5100 建一张采购申请，数量 10，交货日期 2026-09-30",
      "expected": {
        "decisionType": "SELECT",
        "capabilityId": "MM.PR.CreateDraft",
        "validateCalls": 1,
        "executeCalls": 0
      }
    },
    {
      "id": "user-supplied-wins",
      "userQuery": "给物料 M001 工厂 1000 建 100 EA 采购申请 交货 2026-08-01 采购组 601",
      "expected": {
        "decisionType": "SELECT",
        "capabilityId": "MM.PR.CreateDraft",
        "validateCalls": 1,
        "executeCalls": 0,
        "dryRun": {
          "present": false
        }
      },
      "note": "Task 7.2, live. The user states every value including unit and purchasing_group, so nothing is derivable, the decision stays SELECT and NO plan is authored at all - dryRun.present=False is the strongest available form of 'MM.Material.GetInfo was not pulled and no extra READ happened', because it asserts the ABSENCE of the producer rather than the presence of a literal. Mandatory pair with derived-not-asked / mixed: without this case, 'the user's value won' and 'we never derived anything' would produce the same green result. executeCalls=0 because the capability is a WRITE awaiting Human Approval."
    },
    {
      "id": "derived-and-user-supplied-mixed",
      "userQuery": "给物料 M001 工厂 1000 建 100 EA 采购申请 交货 2026-08-01",
      "expected": {
        "decisionType": "ESCALATE_TO_PLANNER",
        "validateCalls": 0,
        "executeCalls": 0,
        "dryRun": {
          "present": true,
          "nodeCount": 2,
          "nodeCapabilities": [
            "MM.Material.GetInfo",
            "MM.PR.CreateDraft"
          ],
          "parameterProvenance": {
            "MM.PR.CreateDraft": {
              "material": "user_supplied",
              "plant": "user_supplied",
              "quantity": "user_supplied",
              "delivery_date": "user_supplied",
              "unit": "user_supplied",
              "purchasing_group": "capability_derived"
            },
            "MM.Material.GetInfo": {
              "material": "user_supplied",
              "plant": "user_supplied"
            }
          },
          "dataEdges": [
            [
              "node.MM.Material.GetInfo",
              "node.MM.PR.CreateDraft",
              "sapnexus:MaterialInfoFact"
            ]
          ],
          "orderedBefore": [
            [
              "node.MM.Material.GetInfo",
              "node.MM.PR.CreateDraft"
            ]
          ],
          "flagsContain": "approval_required"
        }
      },
      "note": "Task 7.3, live, and the load-bearing case of the suite. The user states unit (EA) and omits purchasing_group, so unit binds from literal and purchasing_group from factField in ONE plan - 用户明说优先 and upstream derivation observed side by side. dataEdges is asserted as an EXACT list because defect 1 (task 5.5) was a DUPLICATE data edge for one (producer, consumer, factType) triple: a containment check would pass straight through it, and a case where all parameters are derived or none are cannot produce the second edge at all. flagsContain approval_required is invariant 5: a derived parameter must not shorten Human Approval. MM.Material.GetInfo's own material/plant are user_supplied because task 5.4b propagates the consumer's keyedBy values into the pulled producer - without that the producer could never execute."
    },
    {
      "id": "upstream-empty-degrades-to-elicitation",
      "pending": true,
      "todo": "PENDING, cause named and now sharper. Two distinct properties were conflated under 'upstream empty'. (a) 'The producer is UNAVAILABLE -> degrade to elicitation' is now live as upstream-unreachable-emits-capability-gap, via the governedCapabilities seam. (b) 'The producer RAN and returned an empty result -> degrade to elicitation' is what remains, and it is execution-time: it can only be observed after the Composition Runtime executes the producer node, and by invariant 2 the Agent never executes. It is additionally blocked by finding G1 - PlanExecutor.resolveParameters skips factField, so no derived value is resolved at all yet, empty or otherwise - and G1 is reserved to defect D4 by invariant 5 because resolving it changes computeInputHash. Prerequisite: D4, then a TS PlanExecutor/projection scenario in frontend/src/runtime where a node result can be made empty. Related and already asserted: agent/tests/test_planner_plan_compiler_v2.py::test_an_unsupplied_consumer_parameter_still_fails_closed shows PARAMETER_SOURCE_MISSING rather than a default or a fabricated value.",
      "userQuery": "给 UNKNOWN-MAT 在 5100 建一张采购申请，数量 10，交货日期 2026-09-30",
      "expected": {
        "decisionType": "CLARIFY",
        "validateCalls": 0,
        "executeCalls": 0
      }
    },
    {
      "id": "upstream-unreachable-emits-capability-gap",
      "userQuery": "给物料 M001 工厂 1000 建 100 EA 采购申请 交货 2026-08-01",
      "expected": {
        "decisionType": "CLARIFY",
        "capabilityId": "MM.PR.CreateDraft",
        "missingParameters": [
          "purchasing_group"
        ],
        "validateCalls": 0,
        "executeCalls": 0,
        "dryRun": {
          "present": false
        }
      },
      "governedCapabilities": [
        "MM.Inventory.GetAvailability",
        "MM.PurchaseOrder.GetList",
        "MM.PR.CreateDraft"
      ],
      "note": "Task 7.5, live via the governedCapabilities seam (run_query already accepted snapshot/sources injection, so no orchestrator change was needed). MM.Material.GetInfo is removed from the governed set, which genuinely changes the snapshot id - the plan is compiled against the registry state this case declares, not against a mocked lookup. DEVIATION FROM THE PLAN'S WORDING, recorded not papered over: task 7.5 says a CapabilityGap is emitted and the run ERRORS. What actually happens is better and is asserted here instead: because the selector honours the governed source set, purchasing_group is not considered derivable, so it stays in missing_parameters and the user is ASKED. The governance red line 7.5 exists to protect - 'an unreachable producer must never become a guessed parameter reaching SAP' - is fully met: validateCalls=0, executeCalls=0, dryRun.present=False, so nothing was attempted and nothing was guessed. Eliciting is not an attempt; it is the opposite. missingParameters names purchasing_group ALONE, not unit, because unit was supplied in the utterance - so this also proves the derivability filter is scoped to what is genuinely underivable rather than disabled wholesale."
    }
  ]
}
