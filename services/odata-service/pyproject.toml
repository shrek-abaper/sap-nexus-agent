[project]
name = "odata-service"
version = "0.1.0"
description = "SAP OData read-only microservice for sap-nexus-agent gateway"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
