# Task 17 Report: Optional grounded LLM rephrase for llm/hybrid CLARIFY

## RED

- Command: `.venv/bin/python -m pytest agent/tests/test_clarify_rendering.py -q`
- Result: `5 failed, 6 passed in 0.55s`
- Failure summary:
  - Four direct `rephrase_clarify` tests failed with `AttributeError: module 'sap_nexus_agent.extraction.clarify' has no attribute 'rephrase_clarify'`.
  - Hybrid wiring test failed because the fake model was called once, not twice, proving the rephrase leg was not yet wired.

## GREEN

- Command: `.venv/bin/python -m pytest agent/tests/test_clarify_rendering.py -q`
- Result: `11 passed in 0.68s`

## Full suite

- Command: `.venv/bin/python -m pytest agent/tests/test_clarify_rendering.py agent/tests -q`
- Result: `15 failed, 1342 passed, 1 skipped in 100.38s (0:01:40)`
- Baseline comparison: matches the requested baseline of `15 failed, 1337 passed, 1 skipped` plus the 5 new passing Task 17 tests.

## Parity harness

- Command: `.venv/bin/python -m pytest agent/tests/test_extraction_parity.py -q`
- Result: `108 passed in 5.81s`
- Rule-mode parity unaffected: yes.

## Commit

- Commit hash: `478c36096d080de9af077778460afcd66b4eaa81`
- Commit message: `feat: optional grounded LLM rephrase for CLARIFY in llm/hybrid modes`

## Changed files

- `agent/sap_nexus_agent/extraction/clarify.py`
- `agent/sap_nexus_agent/llm_intent.py`
- `agent/tests/test_clarify_rendering.py`

## Closed-set safety verification

- Added `test_rephrase_rejects_out_of_scope_field`, which passes `missing=["material"]` while the model returns a question mentioning both `物料编号` and the declared-but-not-missing field display name `工厂`.
- `rephrase_clarify` returned `None`, proving the negative closed-set check rejects declared fields outside the missing set.
- The same suite also verifies malformed payloads, timeout, and raised exceptions fall back to the deterministic template path.

## Concerns

- The report file is intentionally written after the implementation commit so it can include the final commit hash. The implementation commit itself contains the brief-specified three files.
- `agent/sap_nexus_agent/llm_intent.py` remains an inherited oversized module; Task 17 required a minimal LLM-leg wiring change there, and no scope-expanding refactor was performed.

## Risk signals

- Cross-module/cross-subsystem coordinated change: hit. This touched `extraction/clarify.py`, `llm_intent.py`, and intent tests.
- Security-sensitive surface: hit. This touches an LLM prompt/response boundary with declaration-derived field content flowing through an optional model. Mitigation: fail-closed wrapper, strict JSON object shape, missing-field positive check, declared-field negative closed-set check, and template fallback on every failure mode.
- Concurrency/locks/shared mutable state: none.
- Data or schema migration: none.
- Public API contract or external interface change: hit. New internal function `rephrase_clarify(...)` was added with the brief-specified interface; no external API or registry schema change.
- Diff exceeds 200 lines: hit. Staged implementation/test diff was `247 insertions(+), 3 deletions(-)` across 3 files.

## Fix round 1: non-dict payload safety

 - Exact fix diff (unified hunk):

   --- a/agent/sap_nexus_agent/extraction/clarify.py
   +++ b/agent/sap_nexus_agent/extraction/clarify.py
   @@
   -    question = payload.get("question")
   +    # payload must be a JSON object (dict). If the model returned a non-dict
   +    # (list, None, string, int, etc.) treat it as malformed and fail closed.
   +    if not isinstance(payload, dict):
   +        return None
   +    question = payload.get("question")

 - New adversarial tests added (file: agent/tests/test_clarify_rendering.py):
   - test_rephrase_rejects_nondict_payload_none (uses _FakeModel(None))
   - test_rephrase_rejects_nondict_payload_list (uses _FakeModel([[]]))

 - Test run results (actual runs performed during this fix)
   - .venv/bin/python -m pytest agent/tests/test_clarify_rendering.py -q
     -> 13 passed in 0.45s
   - .venv/bin/python -m pytest agent/tests -q
     -> 15 failed, 1344 passed, 1 skipped in 74.16s
   - .venv/bin/python -m pytest agent/tests/test_extraction_parity.py -q
     -> 108 passed in 5.75s

 - Commit
   - Commit hash: `38a1321453613e98ff8a2cde7f9a8895aa700d0b`
   - Commit message: `fix: rephrase_clarify fails closed on non-dict model payloads`

 - Changed files (intent-focused list):
   - agent/sap_nexus_agent/extraction/clarify.py (modified)
   - agent/tests/test_clarify_rendering.py (modified)

 - Concerns / notes
   - I created a new commit (did not amend the prior commit) to keep history simple. The commit contains some autogenerated .omo/run-continuation artifacts from the environment; they were included in the commit by the local tooling. The intent-bearing changes are limited to the two files above.
   - The repository-wide full test-suite still reports 15 failing tests unrelated to this change (they match the pre-change baseline and are outside this fix's scope). The parity harness remains green (108/108).

 - Risk signals
   - Low: this is a minimal defensive change (type-check the model payload) and two small tests. The LLM boundary is now more robust against non-object JSON payloads.
   - Medium: incidental .omo artifacts were committed; consider cleaning them from history if undesired.
